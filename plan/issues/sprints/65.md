---
sprint: 65
status: active
---

# Sprint 65 — architecture epics + value-rep substrate (architect-led)

> Carried from s64 (closed 2026-06-21). The standalone-conformance quick-win pool
> drained in s64; s65 pivots to the **architect-led** substrate and epic work.

## Spine: the value-rep dynamic-read substrate (#2580, staged M0–M4)

The single highest-leverage lever — **~390 (floor) / ~1030 (ceiling) test262
rows** — and the common root behind the s64-parked tail (S2/S3/S4 sparse-array,
#2573 open-object, #983d host-method dispatch all converge on the dense/typed
representation's inability to model dynamic / prototype-chain reads). Scoping doc:
`plan/issues/2580-dynamic-receiver-length-undefined-substrate.md`.

Incremental + full-gate-validated migration (NO #1844 big-bang):
- **M0 ✓ MERGED** (#1880) — `__dyn_has`/`__dyn_get` runtime read primitives
  (delegate to `__extern_get`'s prototype-chain walk) + `ctx.usesDynRead` gate.
  Provably-inert 0-risk scaffold; caught the #2043-class type-space desync early.
- **M1 (in flight)** — `any`-receiver `.length` → `__dyn_get` (the ~12-row
  canary). **STOP-THE-LINE if it ejects on a hidden typed-`.length` case** — that
  is the gating-wrong signal that gates the whole substrate's viability.
- **M2** — generic `Array.prototype.X.call(arraylike, cb)` over the primitives
  (~640 rows). **Subsumes #983d** (re-validated real: 40-fail S15.4.4 cluster, a
  ready test corpus); coordinate with the #983d corpus. sd-3 re-spawns here.
- **M3** — prototype-chain `HasProperty` for indexed reads (~350 rows + the #2001
  S2 visit-skip, now correctly gated).
- **M4** — `delete arr[i]` + `in`/`hasOwnProperty` over the dynamic read (the
  delete/presence tail; retro-unblocks #2001 S3).

Substrate stays **solo (value-rep senior-dev) through M1** — M1 validates the
representation; parallelize M2/M3 only after it passes.

## Acorn dogfood endgame (carries from s64)

Tokenizer identity loop CLOSED (#1712) + acorn instantiates (#2582). Remaining
blockers, each a concrete general correctness win: **#2586** (`new this()` in
fnctor static method — in flight), **#2587** (nested-obj-ptrn-default static-method
stack overflow). Continue blocker-by-blocker to full `parse()`.

## Carried from s64 (39 issues)

**Value-rep / object-model substrate (highest leverage — unblocks clusters):**
- #1888 Slice 4 — `$Vec`/string/Map/Set brand arm in `__extern_method_call`
  (plain `(a:any).indexOf` etc.)
- dot-vs-bracket dual-storage; proto-identity through `Object.create`;
  `Object.prototype.toLocaleString` per-type dispatch (new s64 findings — need issues)
- #2029 #2045 #2106 #2151 #2158 #2159 #2160 #2161 #2162 #2175 (standalone residual tails)

**Architecture epics (need architect `## Implementation Plan` first):**
- Async: #1042 (state machine) · #1373b (IR CPS)
- Proxy: #1355 (pure-Wasm traps)
- IR-first refactors: #1916 #1926 #1927 #1930 #2134 #2135 #2138
- Funcidx/pipeline: #1899 #1985
- Self-host: #1712-family · single coercion engine #1917 · #2167

**Other carried:** #1344 #1528 #1919 #2013 #2044 #2083 #2140 #2141 #2146 #2167
#2173 #2181 #2523 #2524 #2527 #742

## Notes
- Spec-first: route every epic + hard substrate issue through an architect spec
  before dev dispatch.
- Durable fork-sync (recurring origin→upstream FF, or branch-from-upstream) to
  stop the staleness that bit s64.

## Results (proposed — pending user close confirmation)

> **Status stays `active`.** This section is the PO's PREPARED wrap-up for the
> tech lead to present; the final close + carry-over flip is gated on the
> user's confirmation. No issue `sprint:` frontmatter has been changed.

**Duration:** `sprint-65/begin` tagged **2026-06-21 20:28 +0200**; wrap-up
prepared **2026-06-24** off `origin/main` HEAD `426e28e8b148` (2026-06-23
23:49 UTC). **382 commits** to `origin/main` since `sprint-65/begin`
(`git rev-list --count sprint-65/begin..origin/main`). Predecessor `sprint/64`
closed 2026-06-21 14:16 UTC — s65 picked up the same day and ran the
architecture-epic spine across ~2 days of the measured window to this wrap-up.

**Conformance (official scope, `benchmarks/results/test262-current.json`,
baseline_sha `21b0b4b2`, 2026-06-23 23:00 UTC):**
**31,771 / 43,135 pass (73.7 %)** — 10,616 fail, 649 compile-error, 81
compile-timeout. (standard 31,172/42,049 · annex_b 599/1,086 · proposals
1,491/4,982.) The architecture-epic spine is substrate work whose payoff is
unlock-shaped, not row-shaped this window; the measured row deltas below are
the few conformance-visible slices that landed.

### Issue throughput

**75 issues carry `sprint: 65` · 43 done · 32 not-done** (14 blocked,
11 in-progress, 6 ready, 1 suspended — on-disk frontmatter is authoritative;
the carried-from-s64 framing in this doc's prose predates several issues being
split/renumbered, e.g. #2580/#2613/#2614/#2585/#2618 now exist as discrete
files). The 32 not-done are architecture-scale substrate epics that inherently
carry forward, exactly as the s64→s65 spine did.

### This session's architecture landings (PR + measured impact)

All landed regression-free against the `merge_group` standalone floor (#2097)
and the per-process sharded test262 floor.

| PR | Issue | What landed | Measured impact |
|----|-------|-------------|-----------------|
| **#1975** | #2580 M3 Stage A | Standalone inline-literal `[[Prototype]]` link for `Object.create({...})` / `setPrototypeOf` — proto chain resolves through an inline object literal | `Object.create({...}).foo` now resolves; substrate slice, 0 regr |
| **#1977** | #2623 (Promise) | Unify class-extends-Promise value-read identity with the capability constructor | **+1 test262 row**, 0 regr |
| **#1981** | #2623-A (async) | Single-box nested capture of an already-boxed outer var (`alreadyBoxed` port) — async-closure capture box-depth | Fixes `illegal cast in Constructor()`, 0 regr |
| **#1984** | #2618 Slice 1 | Proxy START-timing lazy bridge + callable-target wrap (pure runtime) | Proxy apply/construct **14 → 15 (+1)**, 0 regr |
| **#1986** *(in flight, BLOCKED at wrap-up)* | #2580 M3 B-pre | Re-resolve `__is_truthy` funcidx after callback compile — fixes externref-predicate desync in `.call(arrayLike)` for `some`/`every`/`filter` (invalid-Wasm) | Unblocks the M3-B callback-predicate path; open PR, not yet merged |

**Docs / staging / re-grounds (substrate hygiene):**

| PR | Issue | What landed |
|----|-------|-------------|
| #1973 | #2580 M3 Stage A | Spec mis-attributed → **DEFER** (verified per-process) — the spine's first wrong-spec correction |
| #1976 | #2580 M3 Stage B | Stage B scoping — fnctor `new F()` representation decision + continuation handoff |
| #1979 | #2580 M3 Stage B | Stage B re-ground — cluster-composition correction + fnctor-lap blocker confirmed |
| #1974 | #2618 / #2623-C | Slice C re-grounding — **CONFIRM DEFER** (START-timing + `.call()`-on-Proxy + dynamic-`new`, all #56-zone) |

## Retrospective (proposed)

**The defining lesson of s65: an architect-spec-first model produced WRONG
specs three times in a row — and deep-tracing senior-devs with per-process
binaryen verification are what actually shipped regression-free slices.**

1. **Architect-spec-first mis-fired 3× on the substrate.** #2623-A (async box-
   depth), #2623-B (Promise-subclass identity), and #2580 M3 Stage A were each
   handed off with a spec that **mis-attributed the mechanism**. In all three a
   senior-dev had to deep-trace the actual failing path to find the real cause
   (single-box of an already-boxed capture; capability-ctor identity
   unification; inline-literal proto link) before a correct, regression-free
   slice could land. **Takeaway:** for value-rep/substrate work, route through a
   *verify-the-mechanism-first* trace, not a spec written from the issue
   narrative. A spec is a hypothesis until the binaryen output confirms it.

2. **Verify-first, per-process — NEVER in-process loops.** The in-process
   test262 loop falsely reports ~42 `compile_error reading 'kind'` (cross-test
   state bleed); the **per-process sharded runner is the only trustworthy
   signal**. Every s65 landing was validated per-process.

3. **The `merge_group` standalone floor (#2097) earned its keep.** It catches
   broad-impact regressions that PR-level checks miss (standalone floor runs
   only on `merge_group`). Every substrate slice this session was a broad-impact
   change; the floor is what made "0 regr" a measured claim, not a hope.

4. **The M3 framing was corrected mid-sprint.** The earlier "168-row functor
   lap" framing was wrong. The real lever is the **`Object.defineProperty`
   accessor cluster (181 of 266 files)** plus the **already-closed
   `__make_callback` leak**; the functor `.prototype=` lap is only **51 files**
   and is the **hardest / last** slice — escape-analysis-gated with a real
   #1888-floor-eject risk. Sequencing M3 by that corrected cluster composition
   (accessors first, functor-lap last) is the s66 plan, not a row-count lap.

**Process keepers for s66:**
- Substrate epics → trace-first, spec-as-hypothesis; senior-dev (Opus) owns the
  trace, architect documents the *confirmed* mechanism.
- All broad-impact validation through `merge_group` floor + per-process runner.
- One-shot enqueue, never re-enqueue (no merge-queue churn this session).

## Carry-over proposal for Sprint 66 (PROPOSED — frontmatter NOT yet changed)

The 32 not-done `sprint: 65` issues, grouped by epic, each with a one-line
readiness/blocker note. **Proposed** as the s66 architecture-continuation slate;
the user confirms before any `sprint:` flip.

**IR lane (front-end IR adoption / effect model)**
- **#1373b** *(ready, top)* — IR async Phase C: CPS lowering for await/return/throw. Ready; the async IR keystone.
- **#2134** *(blocked)* — IR effect model: classify instruction kinds, enforce program-order. Blocked on IR-capability predicate groundwork.
- **#2135** *(blocked)* — Single IR capability predicate shared by selector + builder. Blocked; pairs with #2134/#2138.
- **#2138** *(blocked)* — IR-first compile-once inversion (selector decides before compileDeclarations). Blocked on #2134/#2135.
- **#2140** *(blocked, critical)* — stack-balance fixBranchType: coerce-or-throw (#1858-C1 split). Blocked on effect model.
- **#2141** *(blocked)* — retire the tag-5 box-the-externref ABI; make consumers tag-agnostic. Blocked on value-rep tag decisions.

**Value-rep / object-model substrate (the #2580 spine)**
- **#2580** *(in-progress)* — `.length` on any/dynamically-mutated receiver returns 0 not undefined. Spine; M3 in flight (B-pre #1986 open), sequence by corrected accessor-cluster composition.
- **#2585** *(blocked)* — `getPrototypeOf(Object.create(p)) === p` is false (object identity). Blocked on the #2580 proto-identity slice.
- **#1917** *(in-progress)* — one coercion engine (four divergent matrices). In progress; core value banked, residual parked.
- **#2106** *(in-progress)* — value-rep P3 undefined observability (UNDEF_F64 sentinel, $undefined singleton). In progress; open PR #1961 (strict ===/!== over nullish any).
- **#2044** *(blocked)* — architect decision: BigInt value representation (i64-brand vs boxing). Blocked on the ValType decision; gates #1644.

**Async / Promise**
- **#1042** *(in-progress)* — async/await state-machine lowering (AwaitExpression no-op). In progress; the async keystone, pairs with #1373b.
- **#2613** *(blocked)* — await on a thenable/non-Promise: assimilate via host PromiseResolve. Blocked on #1042 state machine.
- **#2614** *(blocked)* — Promise.{all,allSettled,any,race}: read ctor's own `resolve`. Blocked on capability-ctor path.
- **promise-async-capability-residual** *(ready)* — NewPromiseCapability(C) for custom constructors + resolve. Ready; banked by #1977's identity unification.

**Proxy**
- **#1355** *(in-progress, top)* — Proxy beyond JS-host fallback (235 fails). In progress; pure-Wasm traps, the big Proxy epic.
- **#2618** *(in-progress)* — calling/constructing a Proxy with callable trap target. In progress; Slice 1 (#1984) landed +1, Slice C confirmed DEFER (#56-zone).

**Standalone residuals**
- **#2160** *(ready)* — String/Number method & coercion residual (~635). Ready; dev-claimable conformance slices.
- **#2161** *(blocked)* — RegExp engine residual (~579). Blocked on the RegExp substrate; some slices landed in-sprint.
- **#2173** *(blocked)* — `yield*` over general iterable in native generators (SF-3 of #2157). Blocked on generator-iterator substrate.
- **#2175** *(in-progress)* — architect spec: builtin-prototype object representation + native-method-closure dispatch. In progress; the standalone-dispatch substrate.
- **#2029** *(in-progress, critical)* — `u32 out of range: -1` on builtin subclassing / Object.create / Iterator.prototype (497). In progress; partial slices landed.
- **#2045** *(in-progress, critical)* — linear Uint8Array silent-corruption holes + escape-analysis demotion. In progress; C.8 slice landed.
- **#2159** *(in-progress)* — TypedArray/DataView/ArrayBuffer residual (~1,308). In progress; several slices landed in-sprint.
- **#2162** *(in-progress)* — Map/Set/WeakMap/WeakSet residual (~532). In progress; slices landed in-sprint.
- **#2158** *(in-progress)* — class/prototype/private-name/descriptor residual (~1,388). In progress; dstr-param slice landed.
- **#2151** *(in-progress)* — any-receiver method dispatch on closed object-literal struct. In progress; pairs with #2175.

**Refactors / pipeline**
- **#742** *(in-progress)* — extract/refactor compileCallExpression (3,350 lines). In progress; long-running structural refactor.
- **#2083** *(ready)* — per-module host-glue suite dominates small-binary size, unstrippable. Ready; size/codegen hygiene.
- **#2181** *(ready)* — defineBuiltin scaffold: unify per-representation element-load/ToString/null. Ready; pairs with #2160/#2088 line.
- **#2146** *(ready)* — retire the registration-indirection layer in codegen/shared.ts. Ready (listed in tables).

**Type-oracle / symbolic refs**
- **#1916** *(blocked)* — symbolic function references; retire late-import index-shift machinery. Blocked on #1930 TypeOracle boundary.
- **#1930** *(blocked)* — TypeOracle: one type-query boundary (unblocks TS7, kills suppression heuristics). Blocked; the type-boundary keystone.
- **#1985** *(blocked)* — stale-proof index cells (#2043 Option 2b). Blocked on #1916 symbolic-ref groundwork.

**Linking / Fable / misc**
- **#2167** *(in-progress)* — Fable model disabled; frontier-reasoning blocked. In progress; gated on type-oracle + value-rep.
- **#2527** *(ready)* — core-wasm module linking (shared store + canonical rec-group) — CHOSEN approach. Ready; the modularization keystone.
- **#1344** *(suspended)* — Generator/AsyncIterator prototype receiver TypeErrors (52+12). Suspended; resumable from issue-file `## Suspended Work`.

<!-- GENERATED_ISSUE_TABLES_START -->
## Issue Tables

_Generated from issue files. Update issue `status`, then rerun `node scripts/sync-sprint-issue-tables.mjs`._

### Blocked

| Issue | Title | Priority | Status |
|---|---|---|---|
| #1916 | Symbolic function references in WasmGC codegen — retire the late-import index-shift machinery | high | blocked |
| #1930 | TypeOracle — one type-query boundary between the TS checker and codegen (unblocks TS7, kills suppression heuristics) | high | blocked |
| #1985 | stale-proof index cells: shift-walker-updated { idx } handles for captured func indices (#2043 Option 2b, incremental) | medium | blocked |
| #2013 | JSON.parse reviver argument silently ignored (parse arm compiles only arguments[0]; host import drops it) | medium | blocked |
| #2044 | architect decision: BigInt value representation — i64-bigint-brand ValType vs TS-type-driven boxing (gates #1644 slices, implicated in #2039 i64 ABI bucket) | high | blocked |
| #2134 | IR effect model: classify instruction kinds, enforce program-order emission for effectful ops | high | blocked |
| #2135 | Single IR capability predicate shared by selector and builder (retire select.ts/from-ast.ts drift) | high | blocked |
| #2138 | IR-first compile-once inversion: selector decides before compileDeclarations (flag-gated investigation) | high | blocked |
| #2140 | stack-balance fixBranchType: coerce-where-possible, throw on impossible (split of #1858-C1) | critical | blocked |
| #2141 | Retire the tag-5 box-the-externref ABI: make consumers tag-agnostic, then allow honest generic boxing | high | blocked |
| #2173 | standalone: yield* over a general iterable (array / custom {next()}) in native generators (SF-3 slice-2 of #2157) | medium | blocked |

### Ready

| Issue | Title | Priority | Status |
|---|---|---|---|
| #1344 | spec gap: Generator/AsyncIterator prototype receiver TypeErrors + return/throw (52 + 12 test262 fails) | medium | ready |
| #1373b | IR async Phase C: CPS lowering for await + async-return + async-throw | top | ready |
| #1528 | spec gap: non-constructor TypeError — Promise.all / allSettled species and executor paths | medium | ready |
| #1899 | finalize funcIdx-authority contract: reconcile↔dead-elim native-string helper sibling-call mismatch (late-shift class recurrence-proofing) | medium | ready |
| #1919 | Transactional speculative-compile API — 23 probe/rollback sites leak locals, imports, and types | medium | ready |
| #1926 | Remove backend ValType/typeIdx from IrType — unions and boxing must be backend-symbolic | medium | ready |
| #1927 | One front-end pipeline driver — compileSourceSync/compileMultiSource/compileFilesSource are divergent clones | high | ready |
| #2083 | per-module exported host-glue suite (__call_fn_*, __sget_*, __vec_*) dominates small-binary size and is unstrippable by wasm-opt | medium | ready |
| #2146 | Retire the registration-indirection layer in codegen/shared.ts | medium | ready |
| #2160 | Standalone String/Number method & coercion conformance residual (~635 tests) | high | ready |
| #2181 | defineBuiltin(name, {elementKinds, lower}) scaffold — unify per-representation element-load/ToString/null handling | medium | ready |
| #2527 | Core-wasm module linking (shared store + canonical rec-group) for host-API shims and the shared runtime — CHOSEN approach | medium | ready |

### In Progress

| Issue | Title | Priority | Status |
|---|---|---|---|
| #742 | Extract and refactor compileCallExpression (3,350 lines) | medium | in-progress |
| #1042 | async/await state-machine lowering (AwaitExpression is currently a no-op) | high | in-progress |
| #1355 | spec backlog: Proxy implementation beyond JS-host fallback (235 test262 fails) | top | in-progress |
| #1917 | One coercion engine — four divergent coercion matrices disagree about lossiness | high | in-progress |
| #2029 | standalone: `Binary emit error: u32 out of range: -1` on builtin subclassing, disposal protocol, Object.create, Iterator.prototype (497 tests) | critical | in-progress |
| #2045 | linear Uint8Array (WASI): silent-corruption holes — name-keyed buffer registry, no bounds checks — plus escape-analysis demotion gaps (#1886 follow-up) | critical | in-progress |
| #2106 | value-rep P3: undefined observability — UNDEF_F64 sentinel, union-collapse reversal (flagged), standalone $undefined singleton | high | in-progress |
| #2151 | standalone: any-receiver method dispatch — o.method() on a closed object-literal struct doesn't invoke | high | in-progress |
| #2158 | Standalone class/prototype/private-name/descriptor conformance residual (~1,388 tests) | high | in-progress |
| #2159 | Standalone TypedArray/DataView/ArrayBuffer conformance residual (~1,308 tests) | high | in-progress |
| #2161 | Standalone RegExp engine conformance residual (~579 tests) | high | in-progress |
| #2162 | Standalone Map/Set/WeakMap/WeakSet conformance residual (~532 tests) | high | in-progress |
| #2167 | Fable model disabled — frontier-reasoning work blocked | high | in-progress |
| #2175 | architect spec: standalone builtin-prototype object representation + native-method-closure dispatch | high | in-progress |
| #2523 | enqueue/unstick must use a GitHub App token, not GITHUB_TOKEN (merge-queue wedge fix) | high | in-progress |
| #2524 | Phase 1: process IO via linkable js2wasm:node-io shim (--node-io-shim) | high | in-progress |

<!-- GENERATED_ISSUE_TABLES_END -->
