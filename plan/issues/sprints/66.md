---
sprint: 66
status: active
---

# Sprint 66 — architecture continuation: value-rep substrate + IR/async/Proxy epics

> Carried from s65 (closed 2026-06-24, user-approved). s65 banked the value-rep
> substrate slices (**#2580** M3 Stage A/B, **#2623** Promise/async identity,
> **#2618** Proxy slice 1), the **#1917 single-coercion-engine series**, and
> drained the standalone quick-win pool (**#2160/#2644/#2151/#2158/#2159/#2162**
> done). s66 continues the same architect-led spine: finish the substrate, land
> the IR effect model, and clear the standalone residual tails.
>
> **Final s65 tally:** 92 issues carried `sprint: 65` → **58 done, 34 carried
> to s66** (reassigned `sprint: 65 → 66` in the close PR), plus **#2637** pulled
> from backlog into the async/Promise group. The 34 carried are the
> architecture-scale substrate epics that inherently span sprints — the same
> shape as the s64→s65 carry. Grouped by epic below; sequence the #2580 M3 spine
> by the corrected cluster composition (accessor cluster FIRST, functor lap LAST).

## Spine: the value-rep dynamic-read substrate (#2580, M3 → M4)

Still the single highest-leverage lever. M0–M2 landed; M3 is in flight
(B-pre callback-predicate funcidx re-resolution). **Sequence M3 by the
corrected cluster composition** (the s65 retro correction):
`Object.defineProperty` accessor cluster first (181 of 266 files) → functor
`.prototype=` lap **last** (51 files, escape-analysis-gated, real #1888-floor
eject risk). Then M4 (`delete arr[i]` + `in`/`hasOwnProperty` over the dynamic
read; retro-unblocks #2001 S3).

- **#2580** *(in-progress)* — `.length` on any/dynamically-mutated receiver. Spine.
- **#2585** *(blocked)* — `getPrototypeOf(Object.create(p)) === p` object identity. Blocked on the #2580 proto-identity slice.
- **#2044** *(blocked)* — architect decision: BigInt value representation (i64-brand vs boxing). Gates #1644.
- **#2106** *(backlog)* — value-rep P3 undefined observability ($undefined singleton, UNDEF_F64 sentinel). Demoted to backlog in s65; reactivate behind #2580.
- **#1917** *(in-progress)* — single coercion engine: core series LANDED in s65; residual parked. Close out the remaining matrices.

## IR lane (front-end IR adoption / effect model)

- **#1373b** *(backlog, top)* — IR async Phase C: CPS lowering for await/return/throw. The async IR keystone.
- **#2134** *(blocked)* — IR effect model: classify instruction kinds, enforce program-order.
- **#2135** *(blocked)* — single IR capability predicate shared by selector + builder.
- **#2138** *(blocked)* — IR-first compile-once inversion. Blocked on #2134/#2135.
- **#2140** *(blocked, critical)* — stack-balance fixBranchType: coerce-or-throw (#1858-C1 split). Blocked on effect model.
- **#2141** *(blocked)* — retire the tag-5 box-the-externref ABI; make consumers tag-agnostic.

## Async / Promise

- **#1042** *(backlog)* — async/await state-machine lowering. The async keystone, pairs with #1373b.
- **#2613** *(blocked)* — await on a thenable/non-Promise: assimilate via host PromiseResolve. Blocked on #1042.
- **#2614** *(blocked)* — Promise.{all,allSettled,any,race}: read ctor's own `resolve`. Blocked on capability-ctor path.
- **#2637** *(ready, pulled from backlog)* — Promise capability executor-body protocol re-architecture (`__promise_subclass_ctor` ↔ `<Sub>_new` ↔ NewPromiseCapability). Banked by s65's #2623 identity unification; the next Promise-subclass slice.
- **promise-async-capability-residual** *(blocked)* — NewPromiseCapability(C) for custom constructors. Blocked on the capability path; pairs with #2637.

## Proxy

- **#1355** *(in-progress, top)* — Proxy beyond JS-host fallback (235 fails). Pure-Wasm traps, the big Proxy epic.
- **#2618** *(blocked)* — calling/constructing a Proxy with callable trap target. Slice 1 (#1984) landed +1 in s65; Slice C confirmed DEFER (#56-zone). Remaining slices blocked on the #56-zone dynamic-new path.

## Standalone residuals (dev-claimable conformance slices)

> #2160 (String/Number residual) and #2151 (any-receiver dispatch) CLOSED during
> the s65 reopen — not carried.

- **#2161** *(blocked)* — RegExp engine residual (~579). Some slices landed in s65.
- **#2173** *(blocked)* — `yield*` over general iterable in native generators (SF-3 of #2157).
- **#2175** *(in-progress)* — architect spec: builtin-prototype object representation + native-method-closure dispatch.
- **#2029** *(in-progress, critical)* — `u32 out of range: -1` on builtin subclassing / Object.create / Iterator.prototype (497).
- **#2045** *(in-progress, critical)* — linear Uint8Array silent-corruption holes + escape-analysis demotion.
- **#2167** *(blocked)* — Fable model disabled; frontier-reasoning blocked. Gated on type-oracle + value-rep.

## Refactors / pipeline / type-oracle

- **#742** *(in-progress)* — extract/refactor compileCallExpression (3,350 lines).
- **#2083** *(ready)* — per-module host-glue suite dominates small-binary size.
- **#2181** *(ready)* — defineBuiltin scaffold: unify per-representation element-load/ToString/null.
- **#2527** *(in-progress)* — core-wasm module linking (shared store + canonical rec-group) — CHOSEN approach.
- **#1916** *(blocked)* — symbolic function references; retire late-import index-shift machinery. Blocked on #1930.
- **#1930** *(blocked)* — TypeOracle: one type-query boundary. The type-boundary keystone.
- **#1985** *(blocked)* — stale-proof index cells (#2043 Option 2b). Blocked on #1916.
- **#1344** *(suspended)* — Generator/AsyncIterator prototype receiver TypeErrors (52+12). Resumable from issue-file `## Suspended Work`.

## Linking / Node-ABI

- **#1772** *(in-progress)* — Node API one-ABI shim across pure-WASI / edge.js→node / JS+WASI hosts (anchor: node:fs readSync/writeSync). Pulled into the s65 reopen; continue the spike.

## Perf (string hot path — s65 reopen pull)

- **#1762** *(ready)* — linear-memory string backing for the build/hash hot path. The string-perf substrate.
- **#2621** *(backlog)* — string-hash AOT-vs-JIT gap: GC-array bounds-check elimination in the hot path. Pairs with #1762.

## Notes

- **Spec-first WITH trace-first** (s65 retro keeper): for value-rep/substrate
  work, route through a *verify-the-mechanism-first* trace — a spec is a
  hypothesis until the binaryen output confirms it. The architect documents the
  *confirmed* mechanism; a senior-dev (Opus) owns the trace.
- All broad-impact validation through the `merge_group` standalone floor (#2097)
  + the per-process sharded runner — NEVER in-process loops.
- One-shot enqueue, never re-enqueue.

<!-- GENERATED_ISSUE_TABLES_START -->
## Issue Tables

_Generated from issue files. Update issue `status`, then rerun `node scripts/sync-sprint-issue-tables.mjs`._

### Backlog

| Issue | Title | Priority | Status |
|---|---|---|---|
| #1042 | async/await state-machine lowering (AwaitExpression is currently a no-op) | high | backlog |
| #1373b | IR async Phase C: CPS lowering for await + async-return + async-throw | top | backlog |
| #2106 | value-rep P3: undefined observability — UNDEF_F64 sentinel, union-collapse reversal (flagged), standalone $undefined singleton | high | backlog |
| #2621 | string-hash AOT-vs-JIT gap: GC-array bounds-check elimination in hot loops (epic) | low | backlog |

### Blocked

| Issue | Title | Priority | Status |
|---|---|---|---|
| #1916 | Symbolic function references in WasmGC codegen — retire the late-import index-shift machinery | high | blocked |
| #1930 | TypeOracle — one type-query boundary between the TS checker and codegen (unblocks TS7, kills suppression heuristics) | high | blocked |
| #1985 | stale-proof index cells: shift-walker-updated { idx } handles for captured func indices (#2043 Option 2b, incremental) | medium | blocked |
| #2044 | architect decision: BigInt value representation — i64-bigint-brand ValType vs TS-type-driven boxing (gates #1644 slices, implicated in #2039 i64 ABI bucket) | high | blocked |
| #2134 | IR effect model: classify instruction kinds, enforce program-order emission for effectful ops | high | blocked |
| #2135 | Single IR capability predicate shared by selector and builder (retire select.ts/from-ast.ts drift) | high | blocked |
| #2138 | IR-first compile-once inversion: selector decides before compileDeclarations (flag-gated investigation) | high | blocked |
| #2140 | stack-balance fixBranchType: coerce-where-possible, throw on impossible (split of #1858-C1) | critical | blocked |
| #2141 | Retire the tag-5 box-the-externref ABI: make consumers tag-agnostic, then allow honest generic boxing | high | blocked |
| #2161 | Standalone RegExp engine conformance residual (~579 tests) | high | blocked |
| #2167 | Fable model disabled — frontier-reasoning work blocked | high | blocked |
| #2173 | standalone: yield* over a general iterable (array / custom {next()}) in native generators (SF-3 slice-2 of #2157) | medium | blocked |
| #2585 | standalone: getPrototypeOf(Object.create(p)) === p is false — object identity lost in __any_strict_eq tag-5 arm | high | blocked |
| #2613 | await on a thenable/non-Promise: assimilate via host (PromiseResolve) instead of returning the raw object (~15 fails) | high | blocked |
| #2614 | Promise.{all,allSettled,any,race}: read constructor's own `resolve` + callable resolve/reject element functions (~45 fails) | medium | blocked |
| #2618 | Proxy (host): calling / constructing a Proxy whose target is callable traps (illegal cast) or ignores the construct trap result (~15 fails) | medium | blocked |

### Ready

| Issue | Title | Priority | Status |
|---|---|---|---|
| #1344 | spec gap: Generator/AsyncIterator prototype receiver TypeErrors + return/throw (52 + 12 test262 fails) | medium | ready |
| #1762 | perf(strings): linear-memory string backing for the build/hash hot path — drop the WasmGC (array i16) GC barrier | high | ready |
| #2083 | per-module exported host-glue suite (__call_fn_*, __sget_*, __vec_*) dominates small-binary size and is unstrippable by wasm-opt | medium | ready |
| #2181 | defineBuiltin(name, {elementKinds, lower}) scaffold — unify per-representation element-load/ToString/null handling | medium | ready |
| #2637 | Promise capability executor-body protocol: __promise_subclass_ctor ↔ <Sub>_new ↔ NewPromiseCapability re-architecture | medium | ready |

### In Progress

| Issue | Title | Priority | Status |
|---|---|---|---|
| #742 | Extract and refactor compileCallExpression (3,350 lines) | medium | in-progress |
| #1355 | spec backlog: Proxy implementation beyond JS-host fallback (235 test262 fails) | top | in-progress |
| #1772 | Node API imports from @types/node, satisfied by one ABI across pure-WASI shim / edge.js→node / JS+WASI hosts (anchor: node:fs readSync/writeSync) | medium | in-progress |
| #1917 | One coercion engine — four divergent coercion matrices disagree about lossiness | high | in-progress |
| #2029 | standalone: `Binary emit error: u32 out of range: -1` on builtin subclassing, disposal protocol, Object.create, Iterator.prototype (497 tests) | critical | in-progress |
| #2045 | linear Uint8Array (WASI): silent-corruption holes — name-keyed buffer registry, no bounds checks — plus escape-analysis demotion gaps (#1886 follow-up) | critical | in-progress |
| #2175 | architect spec: standalone builtin-prototype object representation + native-method-closure dispatch | high | in-progress |
| #2527 | Core-wasm module linking (shared store + canonical rec-group) for host-API shims and the shared runtime — CHOSEN approach | medium | in-progress |
| #2580 | `.length` on an any/dynamically-mutated receiver returns numeric 0, not undefined (runtime property-presence) | medium | in-progress |

<!-- GENERATED_ISSUE_TABLES_END -->
